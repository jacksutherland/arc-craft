{% extends "_layout" %}

{% set titleTag = "Members Portal" %}

{% block main %}

	<section class="gray">
		{% if craft.arc.isUserLoggedIn %}
		{#% if false %#}
			{# USER IS AUTHENTICATED #}
			{% include "members/_index-authenticated" %}
		{% else %}
			{# UNAUTHENTICATED USER #}
			{% include "members/_index-unauthenticated" %}
		{% endif %}
	</section>

{% endblock %}